/// 应用从后台回到前台（或冷启动进入前台）后，触发"自动刷新新内容"的最小间隔。
///
/// 设计意图：短于该间隔的来回切换（例如临时切走去回一条通知）不会触发刷新，
/// 以避免无谓的网络请求与潜在限流；超过该间隔则认为内容可能已过期，回前台时刷新。
///
/// 如需更激进（每次打开都刷新），可下调；如需更保守，可上调。所有需要"打开即刷新"
/// 的页面统一引用本常量，便于一处调参。
const Duration kResumeRefreshMinInterval = Duration(seconds: 30);
